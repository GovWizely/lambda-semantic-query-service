[flake8]
max-line-length = 100
import-order-style = smarkets
application-import-names = service
exclude= venv

[tool:pytest]
ignore = venv
markers =
    focus: to run subset of test suite (select with '-m focus')